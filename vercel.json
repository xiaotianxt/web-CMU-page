{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "npm run build",
    "framework": "nextjs",
    "functions": {
      "app/**/*.{js,ts,jsx,tsx}": {
        "maxDuration": 30
      }
    },
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next"
      }
    ]
  }